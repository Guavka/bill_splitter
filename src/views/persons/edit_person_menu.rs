use crate::store::models::person::ShortPerson;

pub fn edit_person_menu(person: &ShortPerson) {
    /*loop {
        clear_console();

        let store = use_person_store();

        let array = store.get_persons();
        let index = get_index_null("Выберите пользователя", "Назад", &array);
        match index {
            Some(index) => {
            }
            None => return,
        };
    }

     */
}
